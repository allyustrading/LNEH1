const navToggle=document.querySelector('.nav-toggle');const nav=document.querySelector('.nav');if(navToggle){navToggle.addEventListener('click',()=>nav.classList.toggle('open'))}const observer=new IntersectionObserver(entries=>{entries.forEach(e=>{if(e.isIntersecting)e.target.classList.add('visible')})},{threshold:.15});document.querySelectorAll('.reveal,.card,.article-card,.product').forEach(el=>observer.observe(el));document.querySelectorAll('.product button').forEach(btn=>btn.addEventListener('click',()=>{btn.textContent='Added to Demo Cart';setTimeout(()=>btn.textContent='Add to Demo Cart',1400)}));
